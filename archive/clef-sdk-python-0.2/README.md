# Frozen Clef Python 0.2

This directory preserves the pre-Haskell Python SDK and adapter host as
migration evidence. The active `clef-sdk` release is the Cabal package under
`../../clef-sdk`; this archived `pyproject.toml` must not be used for a 0.3
release.
