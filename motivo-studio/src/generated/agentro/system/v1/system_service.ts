// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/system/v1/system_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { Capability } from "../../common/v1/capability";
import { ApiVersionRange, Digest } from "../../common/v1/resource";

export const protobufPackage = "agentro.system.v1";

/** Product names the unchanged public distribution associated with a daemon. */
export enum Product {
  /** PRODUCT_UNSPECIFIED - PRODUCT_UNSPECIFIED is invalid at a validated boundary. */
  PRODUCT_UNSPECIFIED = 0,
  /** PRODUCT_CLEF_SDK - PRODUCT_CLEF_SDK is the clef-sdk distribution. */
  PRODUCT_CLEF_SDK = 1,
  /** PRODUCT_TACTUS_RUNTIME - PRODUCT_TACTUS_RUNTIME is the tactus-runtime distribution. */
  PRODUCT_TACTUS_RUNTIME = 2,
  /** PRODUCT_MOTIVO_STUDIO - PRODUCT_MOTIVO_STUDIO is the motivo-studio distribution. */
  PRODUCT_MOTIVO_STUDIO = 3,
  /** PRODUCT_SEGNO_FLOW - PRODUCT_SEGNO_FLOW is the segno-flow distribution. */
  PRODUCT_SEGNO_FLOW = 4,
  UNRECOGNIZED = -1,
}

/** DaemonKind names one independent Rust daemon failure domain. */
export enum DaemonKind {
  /** DAEMON_KIND_UNSPECIFIED - DAEMON_KIND_UNSPECIFIED is invalid at a validated boundary. */
  DAEMON_KIND_UNSPECIFIED = 0,
  /** DAEMON_KIND_AGENTROD - DAEMON_KIND_AGENTROD owns Clef and agent orchestration. */
  DAEMON_KIND_AGENTROD = 1,
  /** DAEMON_KIND_TACTUSD - DAEMON_KIND_TACTUSD owns execution and workspace transactions. */
  DAEMON_KIND_TACTUSD = 2,
  /** DAEMON_KIND_SEGNOD - DAEMON_KIND_SEGNOD owns schedules, occurrences, and dispatch intent. */
  DAEMON_KIND_SEGNOD = 3,
  UNRECOGNIZED = -1,
}

/** HealthState is the externally visible subset of daemon lifecycle state. */
export enum HealthState {
  /** HEALTH_STATE_UNSPECIFIED - HEALTH_STATE_UNSPECIFIED is invalid at a validated boundary. */
  HEALTH_STATE_UNSPECIFIED = 0,
  /** HEALTH_STATE_STARTING - HEALTH_STATE_STARTING means the daemon is not accepting work yet. */
  HEALTH_STATE_STARTING = 1,
  /** HEALTH_STATE_SERVING - HEALTH_STATE_SERVING means the daemon may accept authorized work. */
  HEALTH_STATE_SERVING = 2,
  /** HEALTH_STATE_DRAINING - HEALTH_STATE_DRAINING means the daemon rejects new long-running work. */
  HEALTH_STATE_DRAINING = 3,
  UNRECOGNIZED = -1,
}

/** CheckHealthRequest has no fields in API v1. */
export interface CheckHealthRequest {
}

/** CheckHealthResponse wraps the daemon health report. */
export interface CheckHealthResponse {
  /** health is the validated point-in-time report. */
  health?: HealthReport | undefined;
}

/** HealthReport reports lifecycle state at a specific UTC instant. */
export interface HealthReport {
  /** state is the current externally visible lifecycle state. */
  state: HealthState;
  /** checked_at is the UTC instant at which state was sampled. */
  checkedAt?: string | undefined;
}

/** GetServerInfoRequest has no fields in API v1. */
export interface GetServerInfoRequest {
}

/** GetServerInfoResponse wraps one daemon's compatibility metadata. */
export interface GetServerInfoResponse {
  /** server_info is the validated metadata for the responding instance. */
  serverInfo?: ServerInfo | undefined;
}

/** ServerInfo is the transport DTO used during bootstrap compatibility checks. */
export interface ServerInfo {
  /** product is the unchanged public distribution name. */
  product: Product;
  /** daemon identifies the independent process failure domain. */
  daemon: DaemonKind;
  /** release_version is the product release version. */
  releaseVersion: string;
  /** api_versions is the compatible API v1 minor range. */
  apiVersions?:
    | ApiVersionRange
    | undefined;
  /** build_commit is present when source provenance is available. */
  buildCommit?:
    | string
    | undefined;
  /** target_triple identifies the compiled Rust target. */
  targetTriple: string;
  /** protocol_descriptor is the fingerprint of the published descriptor set. */
  protocolDescriptor?:
    | Digest
    | undefined;
  /** capabilities contains at most 128 unique, sorted capability names. */
  capabilities: Capability[];
  /** instance_id is an opaque UUIDv7 rotated on every daemon launch. */
  instanceId: string;
  /** started_at is the UTC instant at which this instance started. */
  startedAt?: string | undefined;
}

function createBaseCheckHealthRequest(): CheckHealthRequest {
  return {};
}

export const CheckHealthRequest: MessageFns<CheckHealthRequest> = {
  encode(_: CheckHealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckHealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CheckHealthRequest>, I>>(base?: I): CheckHealthRequest {
    return CheckHealthRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<CheckHealthRequest>, I>>(_: I): CheckHealthRequest {
    const message = createBaseCheckHealthRequest();
    return message;
  },
};

function createBaseCheckHealthResponse(): CheckHealthResponse {
  return { health: undefined };
}

export const CheckHealthResponse: MessageFns<CheckHealthResponse> = {
  encode(message: CheckHealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.health !== undefined) {
      HealthReport.encode(message.health, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckHealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.health = HealthReport.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CheckHealthResponse>, I>>(base?: I): CheckHealthResponse {
    return CheckHealthResponse.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<CheckHealthResponse>, I>>(object: I): CheckHealthResponse {
    const message = createBaseCheckHealthResponse();
    message.health = (object.health !== undefined && object.health !== null)
      ? HealthReport.fromPartial(object.health)
      : undefined;
    return message;
  },
};

function createBaseHealthReport(): HealthReport {
  return { state: 0, checkedAt: undefined };
}

export const HealthReport: MessageFns<HealthReport> = {
  encode(message: HealthReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.checkedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.checkedAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as never;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.checkedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HealthReport>, I>>(base?: I): HealthReport {
    return HealthReport.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<HealthReport>, I>>(object: I): HealthReport {
    const message = createBaseHealthReport();
    message.state = object.state ?? 0;
    message.checkedAt = object.checkedAt ?? undefined;
    return message;
  },
};

function createBaseGetServerInfoRequest(): GetServerInfoRequest {
  return {};
}

export const GetServerInfoRequest: MessageFns<GetServerInfoRequest> = {
  encode(_: GetServerInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetServerInfoRequest>, I>>(base?: I): GetServerInfoRequest {
    return GetServerInfoRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerInfoRequest>, I>>(_: I): GetServerInfoRequest {
    const message = createBaseGetServerInfoRequest();
    return message;
  },
};

function createBaseGetServerInfoResponse(): GetServerInfoResponse {
  return { serverInfo: undefined };
}

export const GetServerInfoResponse: MessageFns<GetServerInfoResponse> = {
  encode(message: GetServerInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverInfo !== undefined) {
      ServerInfo.encode(message.serverInfo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverInfo = ServerInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetServerInfoResponse>, I>>(base?: I): GetServerInfoResponse {
    return GetServerInfoResponse.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerInfoResponse>, I>>(object: I): GetServerInfoResponse {
    const message = createBaseGetServerInfoResponse();
    message.serverInfo = (object.serverInfo !== undefined && object.serverInfo !== null)
      ? ServerInfo.fromPartial(object.serverInfo)
      : undefined;
    return message;
  },
};

function createBaseServerInfo(): ServerInfo {
  return {
    product: 0,
    daemon: 0,
    releaseVersion: "",
    apiVersions: undefined,
    buildCommit: undefined,
    targetTriple: "",
    protocolDescriptor: undefined,
    capabilities: [],
    instanceId: "",
    startedAt: undefined,
  };
}

export const ServerInfo: MessageFns<ServerInfo> = {
  encode(message: ServerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.product !== 0) {
      writer.uint32(8).int32(message.product);
    }
    if (message.daemon !== 0) {
      writer.uint32(16).int32(message.daemon);
    }
    if (message.releaseVersion !== "") {
      writer.uint32(26).string(message.releaseVersion);
    }
    if (message.apiVersions !== undefined) {
      ApiVersionRange.encode(message.apiVersions, writer.uint32(34).fork()).join();
    }
    if (message.buildCommit !== undefined) {
      writer.uint32(42).string(message.buildCommit);
    }
    if (message.targetTriple !== "") {
      writer.uint32(50).string(message.targetTriple);
    }
    if (message.protocolDescriptor !== undefined) {
      Digest.encode(message.protocolDescriptor, writer.uint32(58).fork()).join();
    }
    for (const v of message.capabilities) {
      Capability.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.instanceId !== "") {
      writer.uint32(74).string(message.instanceId);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.product = reader.int32() as never;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.daemon = reader.int32() as never;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.releaseVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.apiVersions = ApiVersionRange.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.buildCommit = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.targetTriple = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.protocolDescriptor = Digest.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.capabilities.push(Capability.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerInfo>, I>>(base?: I): ServerInfo {
    return ServerInfo.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ServerInfo>, I>>(object: I): ServerInfo {
    const message = createBaseServerInfo();
    message.product = object.product ?? 0;
    message.daemon = object.daemon ?? 0;
    message.releaseVersion = object.releaseVersion ?? "";
    message.apiVersions = (object.apiVersions !== undefined && object.apiVersions !== null)
      ? ApiVersionRange.fromPartial(object.apiVersions)
      : undefined;
    message.buildCommit = object.buildCommit ?? undefined;
    message.targetTriple = object.targetTriple ?? "";
    message.protocolDescriptor = (object.protocolDescriptor !== undefined && object.protocolDescriptor !== null)
      ? Digest.fromPartial(object.protocolDescriptor)
      : undefined;
    message.capabilities = object.capabilities?.map((e) => Capability.fromPartial(e)) || [];
    message.instanceId = object.instanceId ?? "";
    message.startedAt = object.startedAt ?? undefined;
    return message;
  },
};

/** SystemService exposes only daemon liveness and compatibility metadata. */
export type SystemServiceService = typeof SystemServiceService;
export const SystemServiceService = {
  /** CheckHealth returns a point-in-time lifecycle health report. */
  checkHealth: {
    path: "/agentro.system.v1.SystemService/CheckHealth" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CheckHealthRequest): Buffer => Buffer.from(CheckHealthRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CheckHealthRequest => CheckHealthRequest.decode(value),
    responseSerialize: (value: CheckHealthResponse): Buffer => Buffer.from(CheckHealthResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CheckHealthResponse => CheckHealthResponse.decode(value),
  },
  /** GetServerInfo returns version and negotiated capability metadata. */
  getServerInfo: {
    path: "/agentro.system.v1.SystemService/GetServerInfo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetServerInfoRequest): Buffer => Buffer.from(GetServerInfoRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetServerInfoRequest => GetServerInfoRequest.decode(value),
    responseSerialize: (value: GetServerInfoResponse): Buffer =>
      Buffer.from(GetServerInfoResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetServerInfoResponse => GetServerInfoResponse.decode(value),
  },
} as const;

export interface SystemServiceServer extends UntypedServiceImplementation {
  /** CheckHealth returns a point-in-time lifecycle health report. */
  checkHealth: handleUnaryCall<CheckHealthRequest, CheckHealthResponse>;
  /** GetServerInfo returns version and negotiated capability metadata. */
  getServerInfo: handleUnaryCall<GetServerInfoRequest, GetServerInfoResponse>;
}

export interface SystemServiceClient extends Client {
  /** CheckHealth returns a point-in-time lifecycle health report. */
  checkHealth(
    request: CheckHealthRequest,
    callback: (error: ServiceError | null, response: CheckHealthResponse) => void,
  ): ClientUnaryCall;
  checkHealth(
    request: CheckHealthRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CheckHealthResponse) => void,
  ): ClientUnaryCall;
  checkHealth(
    request: CheckHealthRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CheckHealthResponse) => void,
  ): ClientUnaryCall;
  /** GetServerInfo returns version and negotiated capability metadata. */
  getServerInfo(
    request: GetServerInfoRequest,
    callback: (error: ServiceError | null, response: GetServerInfoResponse) => void,
  ): ClientUnaryCall;
  getServerInfo(
    request: GetServerInfoRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetServerInfoResponse) => void,
  ): ClientUnaryCall;
  getServerInfo(
    request: GetServerInfoRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetServerInfoResponse) => void,
  ): ClientUnaryCall;
}

export const SystemServiceClient = makeGenericClientConstructor(
  SystemServiceService,
  "agentro.system.v1.SystemService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): SystemServiceClient;
  service: typeof SystemServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
