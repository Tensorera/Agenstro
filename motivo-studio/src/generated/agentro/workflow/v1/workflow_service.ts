// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/workflow/v1/workflow_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "agentro.workflow.v1";

/** ArtifactKind identifies one stable artifact form. */
export enum ArtifactKind {
  /** ARTIFACT_KIND_UNSPECIFIED - ARTIFACT_KIND_UNSPECIFIED is invalid at a validated boundary. */
  ARTIFACT_KIND_UNSPECIFIED = 0,
  /** ARTIFACT_KIND_FILE - ARTIFACT_KIND_FILE is one physical file. */
  ARTIFACT_KIND_FILE = 1,
  /** ARTIFACT_KIND_DIRECTORY - ARTIFACT_KIND_DIRECTORY is one physical directory tree. */
  ARTIFACT_KIND_DIRECTORY = 2,
  /** ARTIFACT_KIND_TEXT - ARTIFACT_KIND_TEXT is UTF-8 text content. */
  ARTIFACT_KIND_TEXT = 3,
  /** ARTIFACT_KIND_JSON - ARTIFACT_KIND_JSON is strict JSON content. */
  ARTIFACT_KIND_JSON = 4,
  UNRECOGNIZED = -1,
}

/** PromptRole identifies the purpose of one ordered prompt fragment. */
export enum PromptRole {
  /** PROMPT_ROLE_UNSPECIFIED - PROMPT_ROLE_UNSPECIFIED is invalid at a validated boundary. */
  PROMPT_ROLE_UNSPECIFIED = 0,
  /** PROMPT_ROLE_POLICY - PROMPT_ROLE_POLICY carries a mandatory policy fragment. */
  PROMPT_ROLE_POLICY = 1,
  /** PROMPT_ROLE_CONTEXT - PROMPT_ROLE_CONTEXT carries read-only task context. */
  PROMPT_ROLE_CONTEXT = 2,
  /** PROMPT_ROLE_INSTRUCTION - PROMPT_ROLE_INSTRUCTION carries the requested work. */
  PROMPT_ROLE_INSTRUCTION = 3,
  /** PROMPT_ROLE_REPAIR - PROMPT_ROLE_REPAIR carries bounded repair guidance. */
  PROMPT_ROLE_REPAIR = 4,
  UNRECOGNIZED = -1,
}

/** EffectKind identifies declared task intent, never an authorization grant. */
export enum EffectKind {
  /** EFFECT_KIND_UNSPECIFIED - EFFECT_KIND_UNSPECIFIED is invalid at a validated boundary. */
  EFFECT_KIND_UNSPECIFIED = 0,
  /** EFFECT_KIND_READ - EFFECT_KIND_READ declares project-content reads. */
  EFFECT_KIND_READ = 1,
  /** EFFECT_KIND_CREATE - EFFECT_KIND_CREATE declares project-content creation. */
  EFFECT_KIND_CREATE = 2,
  /** EFFECT_KIND_MODIFY - EFFECT_KIND_MODIFY declares project-content modification. */
  EFFECT_KIND_MODIFY = 3,
  /** EFFECT_KIND_MOVE - EFFECT_KIND_MOVE declares project-content moves. */
  EFFECT_KIND_MOVE = 4,
  /** EFFECT_KIND_DELETE - EFFECT_KIND_DELETE declares project-content deletion. */
  EFFECT_KIND_DELETE = 5,
  /** EFFECT_KIND_SHELL - EFFECT_KIND_SHELL declares supervised command execution. */
  EFFECT_KIND_SHELL = 6,
  /** EFFECT_KIND_NETWORK - EFFECT_KIND_NETWORK declares network intent. */
  EFFECT_KIND_NETWORK = 7,
  UNRECOGNIZED = -1,
}

/** Effort is a logical Clef route independent of provider-native variants. */
export enum Effort {
  /** EFFORT_UNSPECIFIED - EFFORT_UNSPECIFIED means the default configured route. */
  EFFORT_UNSPECIFIED = 0,
  /** EFFORT_XHIGH - EFFORT_XHIGH selects the xhigh logical route. */
  EFFORT_XHIGH = 1,
  /** EFFORT_HIGH - EFFORT_HIGH selects the high logical route. */
  EFFORT_HIGH = 2,
  /** EFFORT_MEDIUM - EFFORT_MEDIUM selects the medium logical route. */
  EFFORT_MEDIUM = 3,
  /** EFFORT_LOW - EFFORT_LOW selects the low logical route. */
  EFFORT_LOW = 4,
  UNRECOGNIZED = -1,
}

/** Prompt is one bounded ordered prompt fragment. */
export interface Prompt {
  /** role identifies the fragment purpose. */
  role: PromptRole;
  /** content is bounded UTF-8 text. */
  content: string;
  /** name is a stable optional fragment label. */
  name?:
    | string
    | undefined;
  /** priority is a deterministic ordering hint. */
  priority: number;
}

/** ArtifactSpec declares one task output. */
export interface ArtifactSpec {
  /** name is the task-local output slot. */
  name: string;
  /** description is bounded display text. */
  description: string;
  /** kind identifies the stable artifact form. */
  kind: ArtifactKind;
  /** path is a portable workspace-relative path when physical. */
  path?:
    | string
    | undefined;
  /** required gates successful publication. */
  required: boolean;
}

/** ArtifactRef identifies one immutable external input. */
export interface ArtifactRef {
  /** uri is an authorized opaque artifact handle. */
  uri: string;
  /** description is bounded display text. */
  description: string;
  /** kind identifies the stable artifact form. */
  kind: ArtifactKind;
  /** digest is an algorithm-tagged digest when available. */
  digest?:
    | string
    | undefined;
  /** media_type is a bounded content type when available. */
  mediaType?: string | undefined;
}

/** ArtifactBinding identifies one upstream task output. */
export interface ArtifactBinding {
  /** source_task_id identifies the producer task. */
  sourceTaskId: string;
  /** output_name identifies the producer slot. */
  outputName: string;
}

/** TaskInput binds one task-local input slot. */
export interface TaskInput {
  /** name is the consumer input slot. */
  name: string;
  /** source identifies either an external artifact or an upstream output. */
  source?:
    | //
    /** artifact is an immutable external input. */
    { $case: "artifact"; value: ArtifactRef }
    | //
    /** binding references one upstream task output. */
    { $case: "binding"; value: ArtifactBinding }
    | undefined;
}

/** Effect declares one bounded task intent. */
export interface Effect {
  /** kind identifies the intent category. */
  kind: EffectKind;
  /** path_glob optionally scopes project-content intent. */
  pathGlob?: string | undefined;
}

/** TaskDefinition is one immutable task declaration. */
export interface TaskDefinition {
  /** id is unique within the workflow. */
  id: string;
  /** domain_function is the provider-neutral operation name. */
  domainFunction: string;
  /** prompts are retained in explicit declaration order. */
  prompts: Prompt[];
  /** inputs are bounded named artifact bindings. */
  inputs: TaskInput[];
  /** outputs are bounded named artifact declarations. */
  outputs: ArtifactSpec[];
  /** effects are intent declarations and grant no permission. */
  effects: Effect[];
  /** required_capabilities must be negotiated before execution. */
  requiredCapabilities: string[];
  /** preferred_capabilities may explicitly degrade. */
  preferredCapabilities: string[];
  /** effort selects an optional logical model route. */
  effort?: Effort | undefined;
}

/** WorkflowOutput exposes one task output at workflow scope. */
export interface WorkflowOutput {
  /** name is unique within the workflow. */
  name: string;
  /** source identifies the producing task output. */
  source?: ArtifactBinding | undefined;
}

/** WorkflowPolicy carries bounded scheduling and failure settings. */
export interface WorkflowPolicy {
  /** max_concurrency bounds simultaneously active tasks. */
  maxConcurrency: number;
  /** fail_fast stops new admission after a task failure. */
  failFast: boolean;
  /** max_fan_out bounds direct downstream edges per task. */
  maxFanOut: number;
}

/** WorkflowDefinition is the cross-language Clef builder DTO. */
export interface WorkflowDefinition {
  /** schema_version is the explicit Clef model version. */
  schemaVersion: string;
  /** id is the stable workflow identity. */
  id: string;
  /** tasks remain in explicit declaration order. */
  tasks: TaskDefinition[];
  /** outputs expose selected task outputs. */
  outputs: WorkflowOutput[];
  /** policy contains complete bounded scheduling settings. */
  policy?:
    | WorkflowPolicy
    | undefined;
  /** required_capabilities apply to the complete workflow. */
  requiredCapabilities: string[];
}

/** CompileWorkflowRequest is an idempotent compile command. */
export interface CompileWorkflowRequest {
  /** request_id is the command idempotency key. */
  requestId: string;
  /** workflow is the complete versioned definition. */
  workflow?:
    | WorkflowDefinition
    | undefined;
  /** profile_revision_sha256 binds compilation to immutable profile bytes. */
  profileRevisionSha256: Buffer;
}

/** CompileWorkflowResponse returns an immutable plan reference. */
export interface CompileWorkflowResponse {
  /** workflow_id identifies the compiled workflow. */
  workflowId: string;
  /** plan_digest_sha256 is the canonical plan digest bytes. */
  planDigestSha256: Buffer;
}

function createBasePrompt(): Prompt {
  return { role: 0, content: "", name: undefined, priority: 0 };
}

export const Prompt: MessageFns<Prompt> = {
  encode(message: Prompt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== 0) {
      writer.uint32(8).int32(message.role);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    if (message.name !== undefined) {
      writer.uint32(26).string(message.name);
    }
    if (message.priority !== 0) {
      writer.uint32(32).sint32(message.priority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Prompt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrompt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.role = reader.int32() as never;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.priority = reader.sint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Prompt>, I>>(base?: I): Prompt {
    return Prompt.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Prompt>, I>>(object: I): Prompt {
    const message = createBasePrompt();
    message.role = object.role ?? 0;
    message.content = object.content ?? "";
    message.name = object.name ?? undefined;
    message.priority = object.priority ?? 0;
    return message;
  },
};

function createBaseArtifactSpec(): ArtifactSpec {
  return { name: "", description: "", kind: 0, path: undefined, required: false };
}

export const ArtifactSpec: MessageFns<ArtifactSpec> = {
  encode(message: ArtifactSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.path !== undefined) {
      writer.uint32(34).string(message.path);
    }
    if (message.required !== false) {
      writer.uint32(40).bool(message.required);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as never;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ArtifactSpec>, I>>(base?: I): ArtifactSpec {
    return ArtifactSpec.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactSpec>, I>>(object: I): ArtifactSpec {
    const message = createBaseArtifactSpec();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.kind = object.kind ?? 0;
    message.path = object.path ?? undefined;
    message.required = object.required ?? false;
    return message;
  },
};

function createBaseArtifactRef(): ArtifactRef {
  return { uri: "", description: "", kind: 0, digest: undefined, mediaType: undefined };
}

export const ArtifactRef: MessageFns<ArtifactRef> = {
  encode(message: ArtifactRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.digest !== undefined) {
      writer.uint32(34).string(message.digest);
    }
    if (message.mediaType !== undefined) {
      writer.uint32(42).string(message.mediaType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as never;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.digest = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ArtifactRef>, I>>(base?: I): ArtifactRef {
    return ArtifactRef.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactRef>, I>>(object: I): ArtifactRef {
    const message = createBaseArtifactRef();
    message.uri = object.uri ?? "";
    message.description = object.description ?? "";
    message.kind = object.kind ?? 0;
    message.digest = object.digest ?? undefined;
    message.mediaType = object.mediaType ?? undefined;
    return message;
  },
};

function createBaseArtifactBinding(): ArtifactBinding {
  return { sourceTaskId: "", outputName: "" };
}

export const ArtifactBinding: MessageFns<ArtifactBinding> = {
  encode(message: ArtifactBinding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceTaskId !== "") {
      writer.uint32(10).string(message.sourceTaskId);
    }
    if (message.outputName !== "") {
      writer.uint32(18).string(message.outputName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactBinding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactBinding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sourceTaskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ArtifactBinding>, I>>(base?: I): ArtifactBinding {
    return ArtifactBinding.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactBinding>, I>>(object: I): ArtifactBinding {
    const message = createBaseArtifactBinding();
    message.sourceTaskId = object.sourceTaskId ?? "";
    message.outputName = object.outputName ?? "";
    return message;
  },
};

function createBaseTaskInput(): TaskInput {
  return { name: "", source: undefined };
}

export const TaskInput: MessageFns<TaskInput> = {
  encode(message: TaskInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    switch (message.source?.$case) {
      case "artifact":
        ArtifactRef.encode(message.source.value, writer.uint32(82).fork()).join();
        break;
      case "binding":
        ArtifactBinding.encode(message.source.value, writer.uint32(90).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.source = { $case: "artifact", value: ArtifactRef.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.source = { $case: "binding", value: ArtifactBinding.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<TaskInput>, I>>(base?: I): TaskInput {
    return TaskInput.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<TaskInput>, I>>(object: I): TaskInput {
    const message = createBaseTaskInput();
    message.name = object.name ?? "";
    switch (object.source?.$case) {
      case "artifact": {
        if (object.source?.value !== undefined && object.source?.value !== null) {
          message.source = { $case: "artifact", value: ArtifactRef.fromPartial(object.source.value) };
        }
        break;
      }
      case "binding": {
        if (object.source?.value !== undefined && object.source?.value !== null) {
          message.source = { $case: "binding", value: ArtifactBinding.fromPartial(object.source.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseEffect(): Effect {
  return { kind: 0, pathGlob: undefined };
}

export const Effect: MessageFns<Effect> = {
  encode(message: Effect, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.pathGlob !== undefined) {
      writer.uint32(18).string(message.pathGlob);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Effect {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffect();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.kind = reader.int32() as never;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pathGlob = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Effect>, I>>(base?: I): Effect {
    return Effect.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Effect>, I>>(object: I): Effect {
    const message = createBaseEffect();
    message.kind = object.kind ?? 0;
    message.pathGlob = object.pathGlob ?? undefined;
    return message;
  },
};

function createBaseTaskDefinition(): TaskDefinition {
  return {
    id: "",
    domainFunction: "",
    prompts: [],
    inputs: [],
    outputs: [],
    effects: [],
    requiredCapabilities: [],
    preferredCapabilities: [],
    effort: undefined,
  };
}

export const TaskDefinition: MessageFns<TaskDefinition> = {
  encode(message: TaskDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.domainFunction !== "") {
      writer.uint32(18).string(message.domainFunction);
    }
    for (const v of message.prompts) {
      Prompt.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.inputs) {
      TaskInput.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.outputs) {
      ArtifactSpec.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.effects) {
      Effect.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.requiredCapabilities) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.preferredCapabilities) {
      writer.uint32(66).string(v!);
    }
    if (message.effort !== undefined) {
      writer.uint32(72).int32(message.effort);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaskDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaskDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domainFunction = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompts.push(Prompt.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.inputs.push(TaskInput.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.outputs.push(ArtifactSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.effects.push(Effect.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.requiredCapabilities.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.preferredCapabilities.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.effort = reader.int32() as never;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<TaskDefinition>, I>>(base?: I): TaskDefinition {
    return TaskDefinition.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<TaskDefinition>, I>>(object: I): TaskDefinition {
    const message = createBaseTaskDefinition();
    message.id = object.id ?? "";
    message.domainFunction = object.domainFunction ?? "";
    message.prompts = object.prompts?.map((e) => Prompt.fromPartial(e)) || [];
    message.inputs = object.inputs?.map((e) => TaskInput.fromPartial(e)) || [];
    message.outputs = object.outputs?.map((e) => ArtifactSpec.fromPartial(e)) || [];
    message.effects = object.effects?.map((e) => Effect.fromPartial(e)) || [];
    message.requiredCapabilities = object.requiredCapabilities?.map((e) => e) || [];
    message.preferredCapabilities = object.preferredCapabilities?.map((e) => e) || [];
    message.effort = object.effort ?? undefined;
    return message;
  },
};

function createBaseWorkflowOutput(): WorkflowOutput {
  return { name: "", source: undefined };
}

export const WorkflowOutput: MessageFns<WorkflowOutput> = {
  encode(message: WorkflowOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.source !== undefined) {
      ArtifactBinding.encode(message.source, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source = ArtifactBinding.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkflowOutput>, I>>(base?: I): WorkflowOutput {
    return WorkflowOutput.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowOutput>, I>>(object: I): WorkflowOutput {
    const message = createBaseWorkflowOutput();
    message.name = object.name ?? "";
    message.source = (object.source !== undefined && object.source !== null)
      ? ArtifactBinding.fromPartial(object.source)
      : undefined;
    return message;
  },
};

function createBaseWorkflowPolicy(): WorkflowPolicy {
  return { maxConcurrency: 0, failFast: false, maxFanOut: 0 };
}

export const WorkflowPolicy: MessageFns<WorkflowPolicy> = {
  encode(message: WorkflowPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxConcurrency !== 0) {
      writer.uint32(8).uint32(message.maxConcurrency);
    }
    if (message.failFast !== false) {
      writer.uint32(16).bool(message.failFast);
    }
    if (message.maxFanOut !== 0) {
      writer.uint32(24).uint32(message.maxFanOut);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxConcurrency = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.failFast = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxFanOut = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkflowPolicy>, I>>(base?: I): WorkflowPolicy {
    return WorkflowPolicy.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowPolicy>, I>>(object: I): WorkflowPolicy {
    const message = createBaseWorkflowPolicy();
    message.maxConcurrency = object.maxConcurrency ?? 0;
    message.failFast = object.failFast ?? false;
    message.maxFanOut = object.maxFanOut ?? 0;
    return message;
  },
};

function createBaseWorkflowDefinition(): WorkflowDefinition {
  return { schemaVersion: "", id: "", tasks: [], outputs: [], policy: undefined, requiredCapabilities: [] };
}

export const WorkflowDefinition: MessageFns<WorkflowDefinition> = {
  encode(message: WorkflowDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.schemaVersion !== "") {
      writer.uint32(10).string(message.schemaVersion);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    for (const v of message.tasks) {
      TaskDefinition.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.outputs) {
      WorkflowOutput.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.policy !== undefined) {
      WorkflowPolicy.encode(message.policy, writer.uint32(42).fork()).join();
    }
    for (const v of message.requiredCapabilities) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.schemaVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tasks.push(TaskDefinition.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.outputs.push(WorkflowOutput.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.policy = WorkflowPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.requiredCapabilities.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkflowDefinition>, I>>(base?: I): WorkflowDefinition {
    return WorkflowDefinition.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowDefinition>, I>>(object: I): WorkflowDefinition {
    const message = createBaseWorkflowDefinition();
    message.schemaVersion = object.schemaVersion ?? "";
    message.id = object.id ?? "";
    message.tasks = object.tasks?.map((e) => TaskDefinition.fromPartial(e)) || [];
    message.outputs = object.outputs?.map((e) => WorkflowOutput.fromPartial(e)) || [];
    message.policy = (object.policy !== undefined && object.policy !== null)
      ? WorkflowPolicy.fromPartial(object.policy)
      : undefined;
    message.requiredCapabilities = object.requiredCapabilities?.map((e) => e) || [];
    return message;
  },
};

function createBaseCompileWorkflowRequest(): CompileWorkflowRequest {
  return { requestId: "", workflow: undefined, profileRevisionSha256: Buffer.alloc(0) };
}

export const CompileWorkflowRequest: MessageFns<CompileWorkflowRequest> = {
  encode(message: CompileWorkflowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.workflow !== undefined) {
      WorkflowDefinition.encode(message.workflow, writer.uint32(18).fork()).join();
    }
    if (message.profileRevisionSha256.length !== 0) {
      writer.uint32(26).bytes(message.profileRevisionSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompileWorkflowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompileWorkflowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workflow = WorkflowDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.profileRevisionSha256 = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CompileWorkflowRequest>, I>>(base?: I): CompileWorkflowRequest {
    return CompileWorkflowRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<CompileWorkflowRequest>, I>>(object: I): CompileWorkflowRequest {
    const message = createBaseCompileWorkflowRequest();
    message.requestId = object.requestId ?? "";
    message.workflow = (object.workflow !== undefined && object.workflow !== null)
      ? WorkflowDefinition.fromPartial(object.workflow)
      : undefined;
    message.profileRevisionSha256 = object.profileRevisionSha256 ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseCompileWorkflowResponse(): CompileWorkflowResponse {
  return { workflowId: "", planDigestSha256: Buffer.alloc(0) };
}

export const CompileWorkflowResponse: MessageFns<CompileWorkflowResponse> = {
  encode(message: CompileWorkflowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowId !== "") {
      writer.uint32(10).string(message.workflowId);
    }
    if (message.planDigestSha256.length !== 0) {
      writer.uint32(18).bytes(message.planDigestSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompileWorkflowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompileWorkflowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.planDigestSha256 = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CompileWorkflowResponse>, I>>(base?: I): CompileWorkflowResponse {
    return CompileWorkflowResponse.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<CompileWorkflowResponse>, I>>(object: I): CompileWorkflowResponse {
    const message = createBaseCompileWorkflowResponse();
    message.workflowId = object.workflowId ?? "";
    message.planDigestSha256 = object.planDigestSha256 ?? Buffer.alloc(0);
    return message;
  },
};

/** WorkflowService compiles versioned Clef definitions into immutable plans. */
export type WorkflowServiceService = typeof WorkflowServiceService;
export const WorkflowServiceService = {
  /** CompileWorkflow validates and deterministically compiles one definition. */
  compileWorkflow: {
    path: "/agentro.workflow.v1.WorkflowService/CompileWorkflow" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CompileWorkflowRequest): Buffer =>
      Buffer.from(CompileWorkflowRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CompileWorkflowRequest => CompileWorkflowRequest.decode(value),
    responseSerialize: (value: CompileWorkflowResponse): Buffer =>
      Buffer.from(CompileWorkflowResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CompileWorkflowResponse => CompileWorkflowResponse.decode(value),
  },
} as const;

export interface WorkflowServiceServer extends UntypedServiceImplementation {
  /** CompileWorkflow validates and deterministically compiles one definition. */
  compileWorkflow: handleUnaryCall<CompileWorkflowRequest, CompileWorkflowResponse>;
}

export interface WorkflowServiceClient extends Client {
  /** CompileWorkflow validates and deterministically compiles one definition. */
  compileWorkflow(
    request: CompileWorkflowRequest,
    callback: (error: ServiceError | null, response: CompileWorkflowResponse) => void,
  ): ClientUnaryCall;
  compileWorkflow(
    request: CompileWorkflowRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CompileWorkflowResponse) => void,
  ): ClientUnaryCall;
  compileWorkflow(
    request: CompileWorkflowRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CompileWorkflowResponse) => void,
  ): ClientUnaryCall;
}

export const WorkflowServiceClient = makeGenericClientConstructor(
  WorkflowServiceService,
  "agentro.workflow.v1.WorkflowService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkflowServiceClient;
  service: typeof WorkflowServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
