// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/workspace/v1/workspace_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "agentro.workspace.v1";

/** EntryKind distinguishes regular files and directories. */
export enum EntryKind {
  /** ENTRY_KIND_UNSPECIFIED - ENTRY_KIND_UNSPECIFIED is invalid at a validated boundary. */
  ENTRY_KIND_UNSPECIFIED = 0,
  /** ENTRY_KIND_FILE - ENTRY_KIND_FILE is a regular file. */
  ENTRY_KIND_FILE = 1,
  /** ENTRY_KIND_DIRECTORY - ENTRY_KIND_DIRECTORY is a directory. */
  ENTRY_KIND_DIRECTORY = 2,
  UNRECOGNIZED = -1,
}

/** RecoveryState is the externally visible recovery lifecycle. */
export enum RecoveryState {
  /** RECOVERY_STATE_UNSPECIFIED - RECOVERY_STATE_UNSPECIFIED is invalid at a validated boundary. */
  RECOVERY_STATE_UNSPECIFIED = 0,
  /** RECOVERY_STATE_AVAILABLE - RECOVERY_STATE_AVAILABLE may be explicitly applied. */
  RECOVERY_STATE_AVAILABLE = 1,
  /** RECOVERY_STATE_APPLIED - RECOVERY_STATE_APPLIED completed successfully. */
  RECOVERY_STATE_APPLIED = 2,
  /** RECOVERY_STATE_CONFLICTED - RECOVERY_STATE_CONFLICTED preserved an external change. */
  RECOVERY_STATE_CONFLICTED = 3,
  /** RECOVERY_STATE_EXPIRED - RECOVERY_STATE_EXPIRED is outside retention. */
  RECOVERY_STATE_EXPIRED = 4,
  UNRECOGNIZED = -1,
}

/** OpenWorkspaceRequest carries a desktop-authorized native path. */
export interface OpenWorkspaceRequest {
  /** request_id is the command idempotency key. */
  requestId: string;
  /** authorized_native_path is allowed only on this desktop boundary. */
  authorizedNativePath: string;
}

/** Workspace is a bounded authorized workspace reference. */
export interface Workspace {
  /** workspace_id is opaque to clients. */
  workspaceId: string;
  /** display_name is bounded user-facing text. */
  displayName: string;
  /** revision identifies the current indexed workspace snapshot. */
  revision: string;
  /** root_entry_id identifies the virtual root directory. */
  rootEntryId: string;
}

/** WorkspaceEntry is one bounded indexed child entry. */
export interface WorkspaceEntry {
  /** entry_id is opaque to clients. */
  entryId: string;
  /** parent_entry_id is absent only at the virtual root. */
  parentEntryId?:
    | string
    | undefined;
  /** name is one portable path component. */
  name: string;
  /** kind identifies file or directory. */
  kind: EntryKind;
  /** size_bytes is meaningful for regular files. */
  sizeBytes: bigint;
  /** language is a bounded editor hint when known. */
  language?:
    | string
    | undefined;
  /** revision is present for mutable files. */
  revision?:
    | string
    | undefined;
  /** read_only reports whether SaveFile is unavailable. */
  readOnly: boolean;
}

/** ListEntriesRequest selects one directory page. */
export interface ListEntriesRequest {
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** parent_entry_id is absent for the virtual root. */
  parentEntryId?:
    | string
    | undefined;
  /** page_size is constrained by the endpoint. */
  pageSize: number;
  /** cursor is opaque and absent for the first page. */
  cursor?: string | undefined;
}

/** ListEntriesResponse contains one bounded directory page. */
export interface ListEntriesResponse {
  /** entries are returned in stable service-defined order. */
  entries: WorkspaceEntry[];
  /** next_cursor is absent when no later page exists. */
  nextCursor?: string | undefined;
}

/** ReadFileRequest identifies one bounded file read. */
export interface ReadFileRequest {
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** entry_id identifies one regular file. */
  entryId: string;
  /** max_bytes is an explicit allocation and response bound. */
  maxBytes: number;
}

/** FileDocument is one bounded file revision. */
export interface FileDocument {
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** entry_id identifies one regular file. */
  entryId: string;
  /** name is the portable display name. */
  name: string;
  /** content contains bounded raw file bytes. */
  content: Buffer;
  /** revision is required for optimistic SaveFile calls. */
  revision: string;
  /** language is a bounded editor hint. */
  language: string;
  /** read_only reports whether SaveFile is unavailable. */
  readOnly: boolean;
  /** binary reports that content is not editor UTF-8 text. */
  binary: boolean;
  /** truncated reports that max_bytes limited the response. */
  truncated: boolean;
}

/** SaveFileRequest performs one optimistic file write. */
export interface SaveFileRequest {
  /** request_id is the command idempotency key. */
  requestId: string;
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** entry_id identifies one regular file. */
  entryId: string;
  /** content contains bounded replacement bytes. */
  content: Buffer;
  /** expected_revision prevents lost updates. */
  expectedRevision: string;
}

/** RecoveryRecord is one bounded recovery summary. */
export interface RecoveryRecord {
  /** recovery_id is opaque to clients. */
  recoveryId: string;
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** label is bounded display text. */
  label: string;
  /** state is the current recovery lifecycle. */
  state: RecoveryState;
  /** created_at is the durable UTC creation instant. */
  createdAt?:
    | string
    | undefined;
  /** changed_files is a bounded summary count. */
  changedFiles: number;
  /** detail is bounded display text when present. */
  detail?: string | undefined;
}

/** ListRecoveriesRequest selects one bounded page. */
export interface ListRecoveriesRequest {
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** page_size is constrained by the endpoint. */
  pageSize: number;
  /** cursor is opaque and absent for the first page. */
  cursor?: string | undefined;
}

/** ListRecoveriesResponse contains one bounded recovery page. */
export interface ListRecoveriesResponse {
  /** recoveries are returned in stable service-defined order. */
  recoveries: RecoveryRecord[];
  /** next_cursor is absent when no later page exists. */
  nextCursor?: string | undefined;
}

/** RecoverWorkspaceRequest applies one explicit recovery record. */
export interface RecoverWorkspaceRequest {
  /** request_id is the command idempotency key. */
  requestId: string;
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** recovery_id identifies the selected recovery record. */
  recoveryId: string;
}

function createBaseOpenWorkspaceRequest(): OpenWorkspaceRequest {
  return { requestId: "", authorizedNativePath: "" };
}

export const OpenWorkspaceRequest: MessageFns<OpenWorkspaceRequest> = {
  encode(message: OpenWorkspaceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.authorizedNativePath !== "") {
      writer.uint32(18).string(message.authorizedNativePath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenWorkspaceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenWorkspaceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.authorizedNativePath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<OpenWorkspaceRequest>, I>>(base?: I): OpenWorkspaceRequest {
    return OpenWorkspaceRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<OpenWorkspaceRequest>, I>>(object: I): OpenWorkspaceRequest {
    const message = createBaseOpenWorkspaceRequest();
    message.requestId = object.requestId ?? "";
    message.authorizedNativePath = object.authorizedNativePath ?? "";
    return message;
  },
};

function createBaseWorkspace(): Workspace {
  return { workspaceId: "", displayName: "", revision: "", rootEntryId: "" };
}

export const Workspace: MessageFns<Workspace> = {
  encode(message: Workspace, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.revision !== "") {
      writer.uint32(26).string(message.revision);
    }
    if (message.rootEntryId !== "") {
      writer.uint32(34).string(message.rootEntryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Workspace {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.revision = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rootEntryId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Workspace>, I>>(base?: I): Workspace {
    return Workspace.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Workspace>, I>>(object: I): Workspace {
    const message = createBaseWorkspace();
    message.workspaceId = object.workspaceId ?? "";
    message.displayName = object.displayName ?? "";
    message.revision = object.revision ?? "";
    message.rootEntryId = object.rootEntryId ?? "";
    return message;
  },
};

function createBaseWorkspaceEntry(): WorkspaceEntry {
  return {
    entryId: "",
    parentEntryId: undefined,
    name: "",
    kind: 0,
    sizeBytes: 0n,
    language: undefined,
    revision: undefined,
    readOnly: false,
  };
}

export const WorkspaceEntry: MessageFns<WorkspaceEntry> = {
  encode(message: WorkspaceEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entryId !== "") {
      writer.uint32(10).string(message.entryId);
    }
    if (message.parentEntryId !== undefined) {
      writer.uint32(18).string(message.parentEntryId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.sizeBytes !== 0n) {
      if (BigInt.asUintN(64, message.sizeBytes) !== message.sizeBytes) {
        throw new globalThis.Error("value provided for field message.sizeBytes of type uint64 too large");
      }
      writer.uint32(40).uint64(message.sizeBytes);
    }
    if (message.language !== undefined) {
      writer.uint32(50).string(message.language);
    }
    if (message.revision !== undefined) {
      writer.uint32(58).string(message.revision);
    }
    if (message.readOnly !== false) {
      writer.uint32(64).bool(message.readOnly);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entryId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.parentEntryId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as never;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sizeBytes = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.revision = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.readOnly = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WorkspaceEntry>, I>>(base?: I): WorkspaceEntry {
    return WorkspaceEntry.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<WorkspaceEntry>, I>>(object: I): WorkspaceEntry {
    const message = createBaseWorkspaceEntry();
    message.entryId = object.entryId ?? "";
    message.parentEntryId = object.parentEntryId ?? undefined;
    message.name = object.name ?? "";
    message.kind = object.kind ?? 0;
    message.sizeBytes = (object.sizeBytes !== undefined && object.sizeBytes !== null) ? BigInt(object.sizeBytes) : 0n;
    message.language = object.language ?? undefined;
    message.revision = object.revision ?? undefined;
    message.readOnly = object.readOnly ?? false;
    return message;
  },
};

function createBaseListEntriesRequest(): ListEntriesRequest {
  return { workspaceId: "", parentEntryId: undefined, pageSize: 0, cursor: undefined };
}

export const ListEntriesRequest: MessageFns<ListEntriesRequest> = {
  encode(message: ListEntriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.parentEntryId !== undefined) {
      writer.uint32(18).string(message.parentEntryId);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).uint32(message.pageSize);
    }
    if (message.cursor !== undefined) {
      writer.uint32(34).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListEntriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListEntriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.parentEntryId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListEntriesRequest>, I>>(base?: I): ListEntriesRequest {
    return ListEntriesRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ListEntriesRequest>, I>>(object: I): ListEntriesRequest {
    const message = createBaseListEntriesRequest();
    message.workspaceId = object.workspaceId ?? "";
    message.parentEntryId = object.parentEntryId ?? undefined;
    message.pageSize = object.pageSize ?? 0;
    message.cursor = object.cursor ?? undefined;
    return message;
  },
};

function createBaseListEntriesResponse(): ListEntriesResponse {
  return { entries: [], nextCursor: undefined };
}

export const ListEntriesResponse: MessageFns<ListEntriesResponse> = {
  encode(message: ListEntriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      WorkspaceEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== undefined) {
      writer.uint32(18).string(message.nextCursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListEntriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListEntriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(WorkspaceEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListEntriesResponse>, I>>(base?: I): ListEntriesResponse {
    return ListEntriesResponse.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ListEntriesResponse>, I>>(object: I): ListEntriesResponse {
    const message = createBaseListEntriesResponse();
    message.entries = object.entries?.map((e) => WorkspaceEntry.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? undefined;
    return message;
  },
};

function createBaseReadFileRequest(): ReadFileRequest {
  return { workspaceId: "", entryId: "", maxBytes: 0 };
}

export const ReadFileRequest: MessageFns<ReadFileRequest> = {
  encode(message: ReadFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.entryId !== "") {
      writer.uint32(18).string(message.entryId);
    }
    if (message.maxBytes !== 0) {
      writer.uint32(24).uint32(message.maxBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entryId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxBytes = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReadFileRequest>, I>>(base?: I): ReadFileRequest {
    return ReadFileRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ReadFileRequest>, I>>(object: I): ReadFileRequest {
    const message = createBaseReadFileRequest();
    message.workspaceId = object.workspaceId ?? "";
    message.entryId = object.entryId ?? "";
    message.maxBytes = object.maxBytes ?? 0;
    return message;
  },
};

function createBaseFileDocument(): FileDocument {
  return {
    workspaceId: "",
    entryId: "",
    name: "",
    content: Buffer.alloc(0),
    revision: "",
    language: "",
    readOnly: false,
    binary: false,
    truncated: false,
  };
}

export const FileDocument: MessageFns<FileDocument> = {
  encode(message: FileDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.entryId !== "") {
      writer.uint32(18).string(message.entryId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.content.length !== 0) {
      writer.uint32(34).bytes(message.content);
    }
    if (message.revision !== "") {
      writer.uint32(42).string(message.revision);
    }
    if (message.language !== "") {
      writer.uint32(50).string(message.language);
    }
    if (message.readOnly !== false) {
      writer.uint32(56).bool(message.readOnly);
    }
    if (message.binary !== false) {
      writer.uint32(64).bool(message.binary);
    }
    if (message.truncated !== false) {
      writer.uint32(72).bool(message.truncated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entryId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.revision = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.readOnly = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.binary = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<FileDocument>, I>>(base?: I): FileDocument {
    return FileDocument.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<FileDocument>, I>>(object: I): FileDocument {
    const message = createBaseFileDocument();
    message.workspaceId = object.workspaceId ?? "";
    message.entryId = object.entryId ?? "";
    message.name = object.name ?? "";
    message.content = object.content ?? Buffer.alloc(0);
    message.revision = object.revision ?? "";
    message.language = object.language ?? "";
    message.readOnly = object.readOnly ?? false;
    message.binary = object.binary ?? false;
    message.truncated = object.truncated ?? false;
    return message;
  },
};

function createBaseSaveFileRequest(): SaveFileRequest {
  return { requestId: "", workspaceId: "", entryId: "", content: Buffer.alloc(0), expectedRevision: "" };
}

export const SaveFileRequest: MessageFns<SaveFileRequest> = {
  encode(message: SaveFileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.entryId !== "") {
      writer.uint32(26).string(message.entryId);
    }
    if (message.content.length !== 0) {
      writer.uint32(34).bytes(message.content);
    }
    if (message.expectedRevision !== "") {
      writer.uint32(42).string(message.expectedRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveFileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveFileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.entryId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expectedRevision = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SaveFileRequest>, I>>(base?: I): SaveFileRequest {
    return SaveFileRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<SaveFileRequest>, I>>(object: I): SaveFileRequest {
    const message = createBaseSaveFileRequest();
    message.requestId = object.requestId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.entryId = object.entryId ?? "";
    message.content = object.content ?? Buffer.alloc(0);
    message.expectedRevision = object.expectedRevision ?? "";
    return message;
  },
};

function createBaseRecoveryRecord(): RecoveryRecord {
  return {
    recoveryId: "",
    workspaceId: "",
    label: "",
    state: 0,
    createdAt: undefined,
    changedFiles: 0,
    detail: undefined,
  };
}

export const RecoveryRecord: MessageFns<RecoveryRecord> = {
  encode(message: RecoveryRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recoveryId !== "") {
      writer.uint32(10).string(message.recoveryId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.label !== "") {
      writer.uint32(26).string(message.label);
    }
    if (message.state !== 0) {
      writer.uint32(32).int32(message.state);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.changedFiles !== 0) {
      writer.uint32(48).uint32(message.changedFiles);
    }
    if (message.detail !== undefined) {
      writer.uint32(58).string(message.detail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecoveryRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecoveryRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recoveryId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.state = reader.int32() as never;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.changedFiles = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RecoveryRecord>, I>>(base?: I): RecoveryRecord {
    return RecoveryRecord.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<RecoveryRecord>, I>>(object: I): RecoveryRecord {
    const message = createBaseRecoveryRecord();
    message.recoveryId = object.recoveryId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.label = object.label ?? "";
    message.state = object.state ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.changedFiles = object.changedFiles ?? 0;
    message.detail = object.detail ?? undefined;
    return message;
  },
};

function createBaseListRecoveriesRequest(): ListRecoveriesRequest {
  return { workspaceId: "", pageSize: 0, cursor: undefined };
}

export const ListRecoveriesRequest: MessageFns<ListRecoveriesRequest> = {
  encode(message: ListRecoveriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.pageSize !== 0) {
      writer.uint32(16).uint32(message.pageSize);
    }
    if (message.cursor !== undefined) {
      writer.uint32(26).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRecoveriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRecoveriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pageSize = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListRecoveriesRequest>, I>>(base?: I): ListRecoveriesRequest {
    return ListRecoveriesRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ListRecoveriesRequest>, I>>(object: I): ListRecoveriesRequest {
    const message = createBaseListRecoveriesRequest();
    message.workspaceId = object.workspaceId ?? "";
    message.pageSize = object.pageSize ?? 0;
    message.cursor = object.cursor ?? undefined;
    return message;
  },
};

function createBaseListRecoveriesResponse(): ListRecoveriesResponse {
  return { recoveries: [], nextCursor: undefined };
}

export const ListRecoveriesResponse: MessageFns<ListRecoveriesResponse> = {
  encode(message: ListRecoveriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.recoveries) {
      RecoveryRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== undefined) {
      writer.uint32(18).string(message.nextCursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRecoveriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRecoveriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recoveries.push(RecoveryRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListRecoveriesResponse>, I>>(base?: I): ListRecoveriesResponse {
    return ListRecoveriesResponse.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ListRecoveriesResponse>, I>>(object: I): ListRecoveriesResponse {
    const message = createBaseListRecoveriesResponse();
    message.recoveries = object.recoveries?.map((e) => RecoveryRecord.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? undefined;
    return message;
  },
};

function createBaseRecoverWorkspaceRequest(): RecoverWorkspaceRequest {
  return { requestId: "", workspaceId: "", recoveryId: "" };
}

export const RecoverWorkspaceRequest: MessageFns<RecoverWorkspaceRequest> = {
  encode(message: RecoverWorkspaceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.recoveryId !== "") {
      writer.uint32(26).string(message.recoveryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecoverWorkspaceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecoverWorkspaceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recoveryId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RecoverWorkspaceRequest>, I>>(base?: I): RecoverWorkspaceRequest {
    return RecoverWorkspaceRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<RecoverWorkspaceRequest>, I>>(object: I): RecoverWorkspaceRequest {
    const message = createBaseRecoverWorkspaceRequest();
    message.requestId = object.requestId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.recoveryId = object.recoveryId ?? "";
    return message;
  },
};

/** WorkspaceService owns authorized workspace file and recovery operations. */
export type WorkspaceServiceService = typeof WorkspaceServiceService;
export const WorkspaceServiceService = {
  /** OpenWorkspace authorizes and opens one desktop-selected native path. */
  openWorkspace: {
    path: "/agentro.workspace.v1.WorkspaceService/OpenWorkspace" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: OpenWorkspaceRequest): Buffer => Buffer.from(OpenWorkspaceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): OpenWorkspaceRequest => OpenWorkspaceRequest.decode(value),
    responseSerialize: (value: Workspace): Buffer => Buffer.from(Workspace.encode(value).finish()),
    responseDeserialize: (value: Buffer): Workspace => Workspace.decode(value),
  },
  /** ListEntries returns one stable bounded directory page. */
  listEntries: {
    path: "/agentro.workspace.v1.WorkspaceService/ListEntries" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListEntriesRequest): Buffer => Buffer.from(ListEntriesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListEntriesRequest => ListEntriesRequest.decode(value),
    responseSerialize: (value: ListEntriesResponse): Buffer => Buffer.from(ListEntriesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListEntriesResponse => ListEntriesResponse.decode(value),
  },
  /** ReadFile reads one bounded file revision. */
  readFile: {
    path: "/agentro.workspace.v1.WorkspaceService/ReadFile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReadFileRequest): Buffer => Buffer.from(ReadFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReadFileRequest => ReadFileRequest.decode(value),
    responseSerialize: (value: FileDocument): Buffer => Buffer.from(FileDocument.encode(value).finish()),
    responseDeserialize: (value: Buffer): FileDocument => FileDocument.decode(value),
  },
  /** SaveFile performs an optimistic revision-checked write. */
  saveFile: {
    path: "/agentro.workspace.v1.WorkspaceService/SaveFile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SaveFileRequest): Buffer => Buffer.from(SaveFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SaveFileRequest => SaveFileRequest.decode(value),
    responseSerialize: (value: FileDocument): Buffer => Buffer.from(FileDocument.encode(value).finish()),
    responseDeserialize: (value: Buffer): FileDocument => FileDocument.decode(value),
  },
  /** ListRecoveries returns one bounded recovery page. */
  listRecoveries: {
    path: "/agentro.workspace.v1.WorkspaceService/ListRecoveries" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListRecoveriesRequest): Buffer =>
      Buffer.from(ListRecoveriesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListRecoveriesRequest => ListRecoveriesRequest.decode(value),
    responseSerialize: (value: ListRecoveriesResponse): Buffer =>
      Buffer.from(ListRecoveriesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListRecoveriesResponse => ListRecoveriesResponse.decode(value),
  },
  /** RecoverWorkspace applies one explicit recovery record. */
  recoverWorkspace: {
    path: "/agentro.workspace.v1.WorkspaceService/RecoverWorkspace" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RecoverWorkspaceRequest): Buffer =>
      Buffer.from(RecoverWorkspaceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RecoverWorkspaceRequest => RecoverWorkspaceRequest.decode(value),
    responseSerialize: (value: RecoveryRecord): Buffer => Buffer.from(RecoveryRecord.encode(value).finish()),
    responseDeserialize: (value: Buffer): RecoveryRecord => RecoveryRecord.decode(value),
  },
} as const;

export interface WorkspaceServiceServer extends UntypedServiceImplementation {
  /** OpenWorkspace authorizes and opens one desktop-selected native path. */
  openWorkspace: handleUnaryCall<OpenWorkspaceRequest, Workspace>;
  /** ListEntries returns one stable bounded directory page. */
  listEntries: handleUnaryCall<ListEntriesRequest, ListEntriesResponse>;
  /** ReadFile reads one bounded file revision. */
  readFile: handleUnaryCall<ReadFileRequest, FileDocument>;
  /** SaveFile performs an optimistic revision-checked write. */
  saveFile: handleUnaryCall<SaveFileRequest, FileDocument>;
  /** ListRecoveries returns one bounded recovery page. */
  listRecoveries: handleUnaryCall<ListRecoveriesRequest, ListRecoveriesResponse>;
  /** RecoverWorkspace applies one explicit recovery record. */
  recoverWorkspace: handleUnaryCall<RecoverWorkspaceRequest, RecoveryRecord>;
}

export interface WorkspaceServiceClient extends Client {
  /** OpenWorkspace authorizes and opens one desktop-selected native path. */
  openWorkspace(
    request: OpenWorkspaceRequest,
    callback: (error: ServiceError | null, response: Workspace) => void,
  ): ClientUnaryCall;
  openWorkspace(
    request: OpenWorkspaceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Workspace) => void,
  ): ClientUnaryCall;
  openWorkspace(
    request: OpenWorkspaceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Workspace) => void,
  ): ClientUnaryCall;
  /** ListEntries returns one stable bounded directory page. */
  listEntries(
    request: ListEntriesRequest,
    callback: (error: ServiceError | null, response: ListEntriesResponse) => void,
  ): ClientUnaryCall;
  listEntries(
    request: ListEntriesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListEntriesResponse) => void,
  ): ClientUnaryCall;
  listEntries(
    request: ListEntriesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListEntriesResponse) => void,
  ): ClientUnaryCall;
  /** ReadFile reads one bounded file revision. */
  readFile(
    request: ReadFileRequest,
    callback: (error: ServiceError | null, response: FileDocument) => void,
  ): ClientUnaryCall;
  readFile(
    request: ReadFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: FileDocument) => void,
  ): ClientUnaryCall;
  readFile(
    request: ReadFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: FileDocument) => void,
  ): ClientUnaryCall;
  /** SaveFile performs an optimistic revision-checked write. */
  saveFile(
    request: SaveFileRequest,
    callback: (error: ServiceError | null, response: FileDocument) => void,
  ): ClientUnaryCall;
  saveFile(
    request: SaveFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: FileDocument) => void,
  ): ClientUnaryCall;
  saveFile(
    request: SaveFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: FileDocument) => void,
  ): ClientUnaryCall;
  /** ListRecoveries returns one bounded recovery page. */
  listRecoveries(
    request: ListRecoveriesRequest,
    callback: (error: ServiceError | null, response: ListRecoveriesResponse) => void,
  ): ClientUnaryCall;
  listRecoveries(
    request: ListRecoveriesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListRecoveriesResponse) => void,
  ): ClientUnaryCall;
  listRecoveries(
    request: ListRecoveriesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListRecoveriesResponse) => void,
  ): ClientUnaryCall;
  /** RecoverWorkspace applies one explicit recovery record. */
  recoverWorkspace(
    request: RecoverWorkspaceRequest,
    callback: (error: ServiceError | null, response: RecoveryRecord) => void,
  ): ClientUnaryCall;
  recoverWorkspace(
    request: RecoverWorkspaceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RecoveryRecord) => void,
  ): ClientUnaryCall;
  recoverWorkspace(
    request: RecoverWorkspaceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RecoveryRecord) => void,
  ): ClientUnaryCall;
}

export const WorkspaceServiceClient = makeGenericClientConstructor(
  WorkspaceServiceService,
  "agentro.workspace.v1.WorkspaceService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkspaceServiceClient;
  service: typeof WorkspaceServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
