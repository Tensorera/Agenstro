// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/common/v1/resource.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "agentro.common.v1";

/** Digest is an algorithm-tagged content digest. */
export interface Digest {
  /** algorithm is the lower-case algorithm name, initially sha256. */
  algorithm: string;
  /** value contains the raw digest bytes, never base64 text. */
  value: Buffer;
}

/** ApiVersion identifies one additive API major and minor version. */
export interface ApiVersion {
  /** major changes only for a breaking API package revision. */
  major: number;
  /** minor advances for compatible additions. */
  minor: number;
}

/** ApiVersionRange states the compatible minor range within one API major. */
export interface ApiVersionRange {
  /** minimum is the oldest compatible API version. */
  minimum?:
    | ApiVersion
    | undefined;
  /** maximum is the newest compatible API version. */
  maximum?: ApiVersion | undefined;
}

function createBaseDigest(): Digest {
  return { algorithm: "", value: Buffer.alloc(0) };
}

export const Digest: MessageFns<Digest> = {
  encode(message: Digest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.algorithm !== "") {
      writer.uint32(10).string(message.algorithm);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Digest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDigest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.algorithm = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Digest>, I>>(base?: I): Digest {
    return Digest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Digest>, I>>(object: I): Digest {
    const message = createBaseDigest();
    message.algorithm = object.algorithm ?? "";
    message.value = object.value ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseApiVersion(): ApiVersion {
  return { major: 0, minor: 0 };
}

export const ApiVersion: MessageFns<ApiVersion> = {
  encode(message: ApiVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.major !== 0) {
      writer.uint32(8).uint32(message.major);
    }
    if (message.minor !== 0) {
      writer.uint32(16).uint32(message.minor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.major = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minor = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ApiVersion>, I>>(base?: I): ApiVersion {
    return ApiVersion.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ApiVersion>, I>>(object: I): ApiVersion {
    const message = createBaseApiVersion();
    message.major = object.major ?? 0;
    message.minor = object.minor ?? 0;
    return message;
  },
};

function createBaseApiVersionRange(): ApiVersionRange {
  return { minimum: undefined, maximum: undefined };
}

export const ApiVersionRange: MessageFns<ApiVersionRange> = {
  encode(message: ApiVersionRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.minimum !== undefined) {
      ApiVersion.encode(message.minimum, writer.uint32(10).fork()).join();
    }
    if (message.maximum !== undefined) {
      ApiVersion.encode(message.maximum, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiVersionRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiVersionRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.minimum = ApiVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.maximum = ApiVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ApiVersionRange>, I>>(base?: I): ApiVersionRange {
    return ApiVersionRange.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ApiVersionRange>, I>>(object: I): ApiVersionRange {
    const message = createBaseApiVersionRange();
    message.minimum = (object.minimum !== undefined && object.minimum !== null)
      ? ApiVersion.fromPartial(object.minimum)
      : undefined;
    message.maximum = (object.maximum !== undefined && object.maximum !== null)
      ? ApiVersion.fromPartial(object.maximum)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
