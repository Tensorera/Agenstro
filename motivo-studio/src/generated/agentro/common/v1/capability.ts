// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/common/v1/capability.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "agentro.common.v1";

/** CapabilityStability describes whether a negotiated capability is stable. */
export enum CapabilityStability {
  /** CAPABILITY_STABILITY_UNSPECIFIED - CAPABILITY_STABILITY_UNSPECIFIED is invalid at a validated boundary. */
  CAPABILITY_STABILITY_UNSPECIFIED = 0,
  /** CAPABILITY_STABILITY_STABLE - CAPABILITY_STABILITY_STABLE is part of the supported contract. */
  CAPABILITY_STABILITY_STABLE = 1,
  /** CAPABILITY_STABILITY_EXPERIMENTAL - CAPABILITY_STABILITY_EXPERIMENTAL may change outside the stable surface. */
  CAPABILITY_STABILITY_EXPERIMENTAL = 2,
  UNRECOGNIZED = -1,
}

/** Capability is one bounded, namespaced feature reported by a daemon. */
export interface Capability {
  /** name is a lower-case, dot-separated capability identifier. */
  name: string;
  /** stability states whether the capability is stable or experimental. */
  stability: CapabilityStability;
}

function createBaseCapability(): Capability {
  return { name: "", stability: 0 };
}

export const Capability: MessageFns<Capability> = {
  encode(message: Capability, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.stability !== 0) {
      writer.uint32(16).int32(message.stability);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capability {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapability();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stability = reader.int32() as never;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Capability>, I>>(base?: I): Capability {
    return Capability.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Capability>, I>>(object: I): Capability {
    const message = createBaseCapability();
    message.name = object.name ?? "";
    message.stability = object.stability ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
