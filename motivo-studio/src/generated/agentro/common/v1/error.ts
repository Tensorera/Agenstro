// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/common/v1/error.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "agentro.common.v1";

/** ErrorDomain identifies the authority that classified an error. */
export enum ErrorDomain {
  /** ERROR_DOMAIN_UNSPECIFIED - ERROR_DOMAIN_UNSPECIFIED is invalid at a validated boundary. */
  ERROR_DOMAIN_UNSPECIFIED = 0,
  /** ERROR_DOMAIN_AGENT - ERROR_DOMAIN_AGENT covers normalized agent orchestration failures. */
  ERROR_DOMAIN_AGENT = 1,
  /** ERROR_DOMAIN_PROCESS - ERROR_DOMAIN_PROCESS covers supervised process failures. */
  ERROR_DOMAIN_PROCESS = 2,
  /** ERROR_DOMAIN_KERNEL - ERROR_DOMAIN_KERNEL covers Python or Jupyter kernel failures. */
  ERROR_DOMAIN_KERNEL = 3,
  /** ERROR_DOMAIN_CHECKPOINT - ERROR_DOMAIN_CHECKPOINT covers workspace checkpoint failures. */
  ERROR_DOMAIN_CHECKPOINT = 4,
  /** ERROR_DOMAIN_STORAGE - ERROR_DOMAIN_STORAGE covers durable storage failures. */
  ERROR_DOMAIN_STORAGE = 5,
  /** ERROR_DOMAIN_SCHEDULER - ERROR_DOMAIN_SCHEDULER covers occurrence and dispatch failures. */
  ERROR_DOMAIN_SCHEDULER = 6,
  /** ERROR_DOMAIN_CONFIG - ERROR_DOMAIN_CONFIG covers configuration failures. */
  ERROR_DOMAIN_CONFIG = 7,
  /** ERROR_DOMAIN_PROTOCOL - ERROR_DOMAIN_PROTOCOL covers incompatible or malformed protocol data. */
  ERROR_DOMAIN_PROTOCOL = 8,
  /** ERROR_DOMAIN_RESOURCE - ERROR_DOMAIN_RESOURCE covers bounded-resource failures. */
  ERROR_DOMAIN_RESOURCE = 9,
  UNRECOGNIZED = -1,
}

/** RetryAdvice tells a client which class of retry, if any, is safe. */
export enum RetryAdvice {
  /** RETRY_ADVICE_UNSPECIFIED - RETRY_ADVICE_UNSPECIFIED is invalid at a validated boundary. */
  RETRY_ADVICE_UNSPECIFIED = 0,
  /** RETRY_ADVICE_NEVER - RETRY_ADVICE_NEVER means automatic retry is not safe. */
  RETRY_ADVICE_NEVER = 1,
  /** RETRY_ADVICE_AFTER_BACKOFF - RETRY_ADVICE_AFTER_BACKOFF permits a bounded, idempotent retry. */
  RETRY_ADVICE_AFTER_BACKOFF = 2,
  /** RETRY_ADVICE_AFTER_USER_ACTION - RETRY_ADVICE_AFTER_USER_ACTION requires an explicit user correction. */
  RETRY_ADVICE_AFTER_USER_ACTION = 3,
  UNRECOGNIZED = -1,
}

/** ErrorDetail carries stable machine fields for a gRPC status detail. */
export interface ErrorDetail {
  /** domain identifies the authority that classified the error. */
  domain: ErrorDomain;
  /** code is an upper-case stable machine code, not a display message. */
  code: string;
  /** retry gives typed retry guidance. */
  retry: RetryAdvice;
  /** trace_id is an opaque UUIDv7 used to locate bounded diagnostics. */
  traceId: string;
}

function createBaseErrorDetail(): ErrorDetail {
  return { domain: 0, code: "", retry: 0, traceId: "" };
}

export const ErrorDetail: MessageFns<ErrorDetail> = {
  encode(message: ErrorDetail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== 0) {
      writer.uint32(8).int32(message.domain);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    if (message.retry !== 0) {
      writer.uint32(24).int32(message.retry);
    }
    if (message.traceId !== "") {
      writer.uint32(34).string(message.traceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorDetail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.domain = reader.int32() as never;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.retry = reader.int32() as never;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ErrorDetail>, I>>(base?: I): ErrorDetail {
    return ErrorDetail.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorDetail>, I>>(object: I): ErrorDetail {
    const message = createBaseErrorDetail();
    message.domain = object.domain ?? 0;
    message.code = object.code ?? "";
    message.retry = object.retry ?? 0;
    message.traceId = object.traceId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
