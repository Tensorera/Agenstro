// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/schedule/v1/schedule_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "agentro.schedule.v1";

/** ScheduleState is the externally visible schedule lifecycle. */
export enum ScheduleState {
  /** SCHEDULE_STATE_UNSPECIFIED - SCHEDULE_STATE_UNSPECIFIED is invalid at a validated boundary. */
  SCHEDULE_STATE_UNSPECIFIED = 0,
  /** SCHEDULE_STATE_DISABLED - SCHEDULE_STATE_DISABLED does not create occurrences. */
  SCHEDULE_STATE_DISABLED = 1,
  /** SCHEDULE_STATE_READY - SCHEDULE_STATE_READY may create or dispatch occurrences. */
  SCHEDULE_STATE_READY = 2,
  /** SCHEDULE_STATE_DISPATCHING - SCHEDULE_STATE_DISPATCHING owns an active dispatch lease. */
  SCHEDULE_STATE_DISPATCHING = 3,
  /** SCHEDULE_STATE_RECOVERY_REQUIRED - SCHEDULE_STATE_RECOVERY_REQUIRED needs reconciliation. */
  SCHEDULE_STATE_RECOVERY_REQUIRED = 4,
  UNRECOGNIZED = -1,
}

/** Schedule is a bounded schedule summary. */
export interface Schedule {
  /** schedule_id is an opaque schedule identifier. */
  scheduleId: string;
  /** task_id identifies the immutable task family. */
  taskId: string;
  /** label is bounded display text. */
  label: string;
  /** cron is the normalized five-field expression. */
  cron: string;
  /** timezone is the explicit IANA timezone identifier. */
  timezone: string;
  /** state is the current schedule lifecycle. */
  state: ScheduleState;
  /** next_fire_at is the next selected UTC instant when enabled. */
  nextFireAt?:
    | string
    | undefined;
  /** last_run_id is the latest orchestration reference when available. */
  lastRunId?: string | undefined;
}

/** ListSchedulesRequest selects one bounded stable page. */
export interface ListSchedulesRequest {
  /** page_size is constrained by the service endpoint. */
  pageSize: number;
  /** cursor is opaque and absent for the first page. */
  cursor?: string | undefined;
}

/** ListSchedulesResponse contains one bounded page. */
export interface ListSchedulesResponse {
  /** schedules are returned in stable service-defined order. */
  schedules: Schedule[];
  /** next_cursor is absent when no later page exists. */
  nextCursor?: string | undefined;
}

function createBaseSchedule(): Schedule {
  return {
    scheduleId: "",
    taskId: "",
    label: "",
    cron: "",
    timezone: "",
    state: 0,
    nextFireAt: undefined,
    lastRunId: undefined,
  };
}

export const Schedule: MessageFns<Schedule> = {
  encode(message: Schedule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scheduleId !== "") {
      writer.uint32(10).string(message.scheduleId);
    }
    if (message.taskId !== "") {
      writer.uint32(18).string(message.taskId);
    }
    if (message.label !== "") {
      writer.uint32(26).string(message.label);
    }
    if (message.cron !== "") {
      writer.uint32(34).string(message.cron);
    }
    if (message.timezone !== "") {
      writer.uint32(42).string(message.timezone);
    }
    if (message.state !== 0) {
      writer.uint32(48).int32(message.state);
    }
    if (message.nextFireAt !== undefined) {
      Timestamp.encode(toTimestamp(message.nextFireAt), writer.uint32(58).fork()).join();
    }
    if (message.lastRunId !== undefined) {
      writer.uint32(66).string(message.lastRunId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Schedule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scheduleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cron = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timezone = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.state = reader.int32() as never;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.nextFireAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastRunId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Schedule>, I>>(base?: I): Schedule {
    return Schedule.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Schedule>, I>>(object: I): Schedule {
    const message = createBaseSchedule();
    message.scheduleId = object.scheduleId ?? "";
    message.taskId = object.taskId ?? "";
    message.label = object.label ?? "";
    message.cron = object.cron ?? "";
    message.timezone = object.timezone ?? "";
    message.state = object.state ?? 0;
    message.nextFireAt = object.nextFireAt ?? undefined;
    message.lastRunId = object.lastRunId ?? undefined;
    return message;
  },
};

function createBaseListSchedulesRequest(): ListSchedulesRequest {
  return { pageSize: 0, cursor: undefined };
}

export const ListSchedulesRequest: MessageFns<ListSchedulesRequest> = {
  encode(message: ListSchedulesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pageSize !== 0) {
      writer.uint32(8).uint32(message.pageSize);
    }
    if (message.cursor !== undefined) {
      writer.uint32(18).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSchedulesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSchedulesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pageSize = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListSchedulesRequest>, I>>(base?: I): ListSchedulesRequest {
    return ListSchedulesRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ListSchedulesRequest>, I>>(object: I): ListSchedulesRequest {
    const message = createBaseListSchedulesRequest();
    message.pageSize = object.pageSize ?? 0;
    message.cursor = object.cursor ?? undefined;
    return message;
  },
};

function createBaseListSchedulesResponse(): ListSchedulesResponse {
  return { schedules: [], nextCursor: undefined };
}

export const ListSchedulesResponse: MessageFns<ListSchedulesResponse> = {
  encode(message: ListSchedulesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.schedules) {
      Schedule.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== undefined) {
      writer.uint32(18).string(message.nextCursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSchedulesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSchedulesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.schedules.push(Schedule.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListSchedulesResponse>, I>>(base?: I): ListSchedulesResponse {
    return ListSchedulesResponse.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<ListSchedulesResponse>, I>>(object: I): ListSchedulesResponse {
    const message = createBaseListSchedulesResponse();
    message.schedules = object.schedules?.map((e) => Schedule.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? undefined;
    return message;
  },
};

/** ScheduleService exposes bounded Segno schedule pages. */
export type ScheduleServiceService = typeof ScheduleServiceService;
export const ScheduleServiceService = {
  /** ListSchedules returns one stable bounded schedule page. */
  listSchedules: {
    path: "/agentro.schedule.v1.ScheduleService/ListSchedules" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSchedulesRequest): Buffer => Buffer.from(ListSchedulesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSchedulesRequest => ListSchedulesRequest.decode(value),
    responseSerialize: (value: ListSchedulesResponse): Buffer =>
      Buffer.from(ListSchedulesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSchedulesResponse => ListSchedulesResponse.decode(value),
  },
} as const;

export interface ScheduleServiceServer extends UntypedServiceImplementation {
  /** ListSchedules returns one stable bounded schedule page. */
  listSchedules: handleUnaryCall<ListSchedulesRequest, ListSchedulesResponse>;
}

export interface ScheduleServiceClient extends Client {
  /** ListSchedules returns one stable bounded schedule page. */
  listSchedules(
    request: ListSchedulesRequest,
    callback: (error: ServiceError | null, response: ListSchedulesResponse) => void,
  ): ClientUnaryCall;
  listSchedules(
    request: ListSchedulesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSchedulesResponse) => void,
  ): ClientUnaryCall;
  listSchedules(
    request: ListSchedulesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSchedulesResponse) => void,
  ): ClientUnaryCall;
}

export const ScheduleServiceClient = makeGenericClientConstructor(
  ScheduleServiceService,
  "agentro.schedule.v1.ScheduleService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): ScheduleServiceClient;
  service: typeof ScheduleServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
