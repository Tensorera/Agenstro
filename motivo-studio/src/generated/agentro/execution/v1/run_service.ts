// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: agentro/execution/v1/run_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "agentro.execution.v1";

/** RunState is the externally visible run lifecycle. */
export enum RunState {
  /** RUN_STATE_UNSPECIFIED - RUN_STATE_UNSPECIFIED is invalid at a validated boundary. */
  RUN_STATE_UNSPECIFIED = 0,
  /** RUN_STATE_QUEUED - RUN_STATE_QUEUED means durable admission completed. */
  RUN_STATE_QUEUED = 1,
  /** RUN_STATE_RUNNING - RUN_STATE_RUNNING means at least one stage is active. */
  RUN_STATE_RUNNING = 2,
  /** RUN_STATE_RECOVERING - RUN_STATE_RECOVERING means reconciliation is in progress. */
  RUN_STATE_RECOVERING = 3,
  /** RUN_STATE_SUCCEEDED - RUN_STATE_SUCCEEDED is a successful terminal state. */
  RUN_STATE_SUCCEEDED = 4,
  /** RUN_STATE_FAILED - RUN_STATE_FAILED is a failed terminal state. */
  RUN_STATE_FAILED = 5,
  /** RUN_STATE_CANCELLED - RUN_STATE_CANCELLED is a cancelled terminal state. */
  RUN_STATE_CANCELLED = 6,
  UNRECOGNIZED = -1,
}

/** Run is a bounded point-in-time workflow snapshot. */
export interface Run {
  /** run_id is an opaque run identifier. */
  runId: string;
  /** workspace_id is the authorized workspace reference. */
  workspaceId: string;
  /** state is the current lifecycle state. */
  state: RunState;
  /** last_sequence is the latest durable event sequence. */
  lastSequence: bigint;
  /** updated_at is the latest durable update instant. */
  updatedAt?:
    | string
    | undefined;
  /** status_detail is a bounded display-only summary when present. */
  statusDetail?: string | undefined;
}

/** StartRunRequest selects a Studio entry in an authorized workspace. */
export interface StartRunRequest {
  /** request_id is the command idempotency key. */
  requestId: string;
  /** workspace_id identifies the authorized workspace. */
  workspaceId: string;
  /** entry_id identifies the versioned workflow or script entry. */
  entryId: string;
}

/** GetRunRequest identifies one run. */
export interface GetRunRequest {
  /** run_id is the opaque run identifier. */
  runId: string;
}

/** WatchRunRequest resumes one run-local sequence. */
export interface WatchRunRequest {
  /** run_id is the opaque run identifier. */
  runId: string;
  /** after_sequence is the last sequence already applied by the client. */
  afterSequence: bigint;
}

/** CancelRunRequest idempotently targets one run. */
export interface CancelRunRequest {
  /** request_id is the command idempotency key. */
  requestId: string;
  /** run_id is the opaque run identifier. */
  runId: string;
}

/** RunStarted is the initial run event. */
export interface RunStarted {
  /** label is a bounded user-facing run label. */
  label: string;
}

/** StageChanged reports one normalized stage transition. */
export interface StageChanged {
  /** stage_id is the stable stage identifier. */
  stageId: string;
  /** label is the bounded display label. */
  label: string;
  /** state is the stable normalized stage state. */
  state: string;
}

/** OutputChunk carries one bounded output fragment. */
export interface OutputChunk {
  /** stream identifies stdout, stderr, or a normalized system stream. */
  stream: string;
  /** data contains raw bounded chunk bytes. */
  data: Buffer;
  /** truncated reports deliberate source-side truncation. */
  truncated: boolean;
}

/** RunDiagnostic carries one bounded stable diagnostic. */
export interface RunDiagnostic {
  /** code is a stable machine-readable code. */
  code: string;
  /** message is bounded display text. */
  message: string;
}

/** RunFinished is the unique terminal event body. */
export interface RunFinished {
  /** state must be a terminal RunState. */
  state: RunState;
  /** summary is bounded display text when present. */
  summary?: string | undefined;
}

/** RunEvent is one run-local, monotonically sequenced event. */
export interface RunEvent {
  /** run_id identifies the owning run. */
  runId: string;
  /** sequence is one-based and contiguous within the run. */
  sequence: bigint;
  /** occurred_at is the normalized UTC event instant. */
  occurredAt?:
    | string
    | undefined;
  /** body contains exactly one normalized event variant. */
  body?:
    | //
    /** started is the initial event. */
    { $case: "started"; value: RunStarted }
    | //
    /** stage_changed reports a stage transition. */
    { $case: "stageChanged"; value: StageChanged }
    | //
    /** output carries bounded output bytes. */
    { $case: "output"; value: OutputChunk }
    | //
    /** diagnostic carries a stable bounded warning or failure detail. */
    { $case: "diagnostic"; value: RunDiagnostic }
    | //
    /** finished is the unique terminal event. */
    { $case: "finished"; value: RunFinished }
    | undefined;
}

function createBaseRun(): Run {
  return { runId: "", workspaceId: "", state: 0, lastSequence: 0n, updatedAt: undefined, statusDetail: undefined };
}

export const Run: MessageFns<Run> = {
  encode(message: Run, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.lastSequence !== 0n) {
      if (BigInt.asUintN(64, message.lastSequence) !== message.lastSequence) {
        throw new globalThis.Error("value provided for field message.lastSequence of type uint64 too large");
      }
      writer.uint32(32).uint64(message.lastSequence);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).join();
    }
    if (message.statusDetail !== undefined) {
      writer.uint32(50).string(message.statusDetail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Run {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRun();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as never;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastSequence = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.statusDetail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Run>, I>>(base?: I): Run {
    return Run.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<Run>, I>>(object: I): Run {
    const message = createBaseRun();
    message.runId = object.runId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.state = object.state ?? 0;
    message.lastSequence = (object.lastSequence !== undefined && object.lastSequence !== null)
      ? BigInt(object.lastSequence)
      : 0n;
    message.updatedAt = object.updatedAt ?? undefined;
    message.statusDetail = object.statusDetail ?? undefined;
    return message;
  },
};

function createBaseStartRunRequest(): StartRunRequest {
  return { requestId: "", workspaceId: "", entryId: "" };
}

export const StartRunRequest: MessageFns<StartRunRequest> = {
  encode(message: StartRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.workspaceId !== "") {
      writer.uint32(18).string(message.workspaceId);
    }
    if (message.entryId !== "") {
      writer.uint32(26).string(message.entryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.entryId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StartRunRequest>, I>>(base?: I): StartRunRequest {
    return StartRunRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<StartRunRequest>, I>>(object: I): StartRunRequest {
    const message = createBaseStartRunRequest();
    message.requestId = object.requestId ?? "";
    message.workspaceId = object.workspaceId ?? "";
    message.entryId = object.entryId ?? "";
    return message;
  },
};

function createBaseGetRunRequest(): GetRunRequest {
  return { runId: "" };
}

export const GetRunRequest: MessageFns<GetRunRequest> = {
  encode(message: GetRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetRunRequest>, I>>(base?: I): GetRunRequest {
    return GetRunRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<GetRunRequest>, I>>(object: I): GetRunRequest {
    const message = createBaseGetRunRequest();
    message.runId = object.runId ?? "";
    return message;
  },
};

function createBaseWatchRunRequest(): WatchRunRequest {
  return { runId: "", afterSequence: 0n };
}

export const WatchRunRequest: MessageFns<WatchRunRequest> = {
  encode(message: WatchRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.afterSequence !== 0n) {
      if (BigInt.asUintN(64, message.afterSequence) !== message.afterSequence) {
        throw new globalThis.Error("value provided for field message.afterSequence of type uint64 too large");
      }
      writer.uint32(16).uint64(message.afterSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WatchRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatchRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.afterSequence = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<WatchRunRequest>, I>>(base?: I): WatchRunRequest {
    return WatchRunRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<WatchRunRequest>, I>>(object: I): WatchRunRequest {
    const message = createBaseWatchRunRequest();
    message.runId = object.runId ?? "";
    message.afterSequence = (object.afterSequence !== undefined && object.afterSequence !== null)
      ? BigInt(object.afterSequence)
      : 0n;
    return message;
  },
};

function createBaseCancelRunRequest(): CancelRunRequest {
  return { requestId: "", runId: "" };
}

export const CancelRunRequest: MessageFns<CancelRunRequest> = {
  encode(message: CancelRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.runId !== "") {
      writer.uint32(18).string(message.runId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CancelRunRequest>, I>>(base?: I): CancelRunRequest {
    return CancelRunRequest.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<CancelRunRequest>, I>>(object: I): CancelRunRequest {
    const message = createBaseCancelRunRequest();
    message.requestId = object.requestId ?? "";
    message.runId = object.runId ?? "";
    return message;
  },
};

function createBaseRunStarted(): RunStarted {
  return { label: "" };
}

export const RunStarted: MessageFns<RunStarted> = {
  encode(message: RunStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.label !== "") {
      writer.uint32(10).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.label = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RunStarted>, I>>(base?: I): RunStarted {
    return RunStarted.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<RunStarted>, I>>(object: I): RunStarted {
    const message = createBaseRunStarted();
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseStageChanged(): StageChanged {
  return { stageId: "", label: "", state: "" };
}

export const StageChanged: MessageFns<StageChanged> = {
  encode(message: StageChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stageId !== "") {
      writer.uint32(10).string(message.stageId);
    }
    if (message.label !== "") {
      writer.uint32(18).string(message.label);
    }
    if (message.state !== "") {
      writer.uint32(26).string(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StageChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStageChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stageId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.state = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StageChanged>, I>>(base?: I): StageChanged {
    return StageChanged.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<StageChanged>, I>>(object: I): StageChanged {
    const message = createBaseStageChanged();
    message.stageId = object.stageId ?? "";
    message.label = object.label ?? "";
    message.state = object.state ?? "";
    return message;
  },
};

function createBaseOutputChunk(): OutputChunk {
  return { stream: "", data: Buffer.alloc(0), truncated: false };
}

export const OutputChunk: MessageFns<OutputChunk> = {
  encode(message: OutputChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stream !== "") {
      writer.uint32(10).string(message.stream);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.truncated !== false) {
      writer.uint32(24).bool(message.truncated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OutputChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutputChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stream = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.truncated = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<OutputChunk>, I>>(base?: I): OutputChunk {
    return OutputChunk.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<OutputChunk>, I>>(object: I): OutputChunk {
    const message = createBaseOutputChunk();
    message.stream = object.stream ?? "";
    message.data = object.data ?? Buffer.alloc(0);
    message.truncated = object.truncated ?? false;
    return message;
  },
};

function createBaseRunDiagnostic(): RunDiagnostic {
  return { code: "", message: "" };
}

export const RunDiagnostic: MessageFns<RunDiagnostic> = {
  encode(message: RunDiagnostic, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunDiagnostic {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunDiagnostic();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RunDiagnostic>, I>>(base?: I): RunDiagnostic {
    return RunDiagnostic.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<RunDiagnostic>, I>>(object: I): RunDiagnostic {
    const message = createBaseRunDiagnostic();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRunFinished(): RunFinished {
  return { state: 0, summary: undefined };
}

export const RunFinished: MessageFns<RunFinished> = {
  encode(message: RunFinished, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.summary !== undefined) {
      writer.uint32(18).string(message.summary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunFinished {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunFinished();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as never;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RunFinished>, I>>(base?: I): RunFinished {
    return RunFinished.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<RunFinished>, I>>(object: I): RunFinished {
    const message = createBaseRunFinished();
    message.state = object.state ?? 0;
    message.summary = object.summary ?? undefined;
    return message;
  },
};

function createBaseRunEvent(): RunEvent {
  return { runId: "", sequence: 0n, occurredAt: undefined, body: undefined };
}

export const RunEvent: MessageFns<RunEvent> = {
  encode(message: RunEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.sequence !== 0n) {
      if (BigInt.asUintN(64, message.sequence) !== message.sequence) {
        throw new globalThis.Error("value provided for field message.sequence of type uint64 too large");
      }
      writer.uint32(16).uint64(message.sequence);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(26).fork()).join();
    }
    switch (message.body?.$case) {
      case "started":
        RunStarted.encode(message.body.value, writer.uint32(82).fork()).join();
        break;
      case "stageChanged":
        StageChanged.encode(message.body.value, writer.uint32(90).fork()).join();
        break;
      case "output":
        OutputChunk.encode(message.body.value, writer.uint32(98).fork()).join();
        break;
      case "diagnostic":
        RunDiagnostic.encode(message.body.value, writer.uint32(106).fork()).join();
        break;
      case "finished":
        RunFinished.encode(message.body.value, writer.uint32(114).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sequence = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.body = { $case: "started", value: RunStarted.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.body = { $case: "stageChanged", value: StageChanged.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.body = { $case: "output", value: OutputChunk.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.body = { $case: "diagnostic", value: RunDiagnostic.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.body = { $case: "finished", value: RunFinished.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RunEvent>, I>>(base?: I): RunEvent {
    return RunEvent.fromPartial(base ?? ({} as never));
  },
  fromPartial<I extends Exact<DeepPartial<RunEvent>, I>>(object: I): RunEvent {
    const message = createBaseRunEvent();
    message.runId = object.runId ?? "";
    message.sequence = (object.sequence !== undefined && object.sequence !== null) ? BigInt(object.sequence) : 0n;
    message.occurredAt = object.occurredAt ?? undefined;
    switch (object.body?.$case) {
      case "started": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "started", value: RunStarted.fromPartial(object.body.value) };
        }
        break;
      }
      case "stageChanged": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "stageChanged", value: StageChanged.fromPartial(object.body.value) };
        }
        break;
      }
      case "output": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "output", value: OutputChunk.fromPartial(object.body.value) };
        }
        break;
      }
      case "diagnostic": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "diagnostic", value: RunDiagnostic.fromPartial(object.body.value) };
        }
        break;
      }
      case "finished": {
        if (object.body?.value !== undefined && object.body?.value !== null) {
          message.body = { $case: "finished", value: RunFinished.fromPartial(object.body.value) };
        }
        break;
      }
    }
    return message;
  },
};

/** RunService controls one bounded workflow run and its resumable event stream. */
export type RunServiceService = typeof RunServiceService;
export const RunServiceService = {
  /** StartRun durably accepts one idempotent run request. */
  startRun: {
    path: "/agentro.execution.v1.RunService/StartRun" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: StartRunRequest): Buffer => Buffer.from(StartRunRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StartRunRequest => StartRunRequest.decode(value),
    responseSerialize: (value: Run): Buffer => Buffer.from(Run.encode(value).finish()),
    responseDeserialize: (value: Buffer): Run => Run.decode(value),
  },
  /** GetRun returns a point-in-time run snapshot. */
  getRun: {
    path: "/agentro.execution.v1.RunService/GetRun" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetRunRequest): Buffer => Buffer.from(GetRunRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetRunRequest => GetRunRequest.decode(value),
    responseSerialize: (value: Run): Buffer => Buffer.from(Run.encode(value).finish()),
    responseDeserialize: (value: Buffer): Run => Run.decode(value),
  },
  /** WatchRun resumes events strictly after the supplied sequence. */
  watchRun: {
    path: "/agentro.execution.v1.RunService/WatchRun" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: WatchRunRequest): Buffer => Buffer.from(WatchRunRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WatchRunRequest => WatchRunRequest.decode(value),
    responseSerialize: (value: RunEvent): Buffer => Buffer.from(RunEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer): RunEvent => RunEvent.decode(value),
  },
  /** CancelRun idempotently requests run cancellation. */
  cancelRun: {
    path: "/agentro.execution.v1.RunService/CancelRun" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CancelRunRequest): Buffer => Buffer.from(CancelRunRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CancelRunRequest => CancelRunRequest.decode(value),
    responseSerialize: (value: Run): Buffer => Buffer.from(Run.encode(value).finish()),
    responseDeserialize: (value: Buffer): Run => Run.decode(value),
  },
} as const;

export interface RunServiceServer extends UntypedServiceImplementation {
  /** StartRun durably accepts one idempotent run request. */
  startRun: handleUnaryCall<StartRunRequest, Run>;
  /** GetRun returns a point-in-time run snapshot. */
  getRun: handleUnaryCall<GetRunRequest, Run>;
  /** WatchRun resumes events strictly after the supplied sequence. */
  watchRun: handleServerStreamingCall<WatchRunRequest, RunEvent>;
  /** CancelRun idempotently requests run cancellation. */
  cancelRun: handleUnaryCall<CancelRunRequest, Run>;
}

export interface RunServiceClient extends Client {
  /** StartRun durably accepts one idempotent run request. */
  startRun(request: StartRunRequest, callback: (error: ServiceError | null, response: Run) => void): ClientUnaryCall;
  startRun(
    request: StartRunRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Run) => void,
  ): ClientUnaryCall;
  startRun(
    request: StartRunRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Run) => void,
  ): ClientUnaryCall;
  /** GetRun returns a point-in-time run snapshot. */
  getRun(request: GetRunRequest, callback: (error: ServiceError | null, response: Run) => void): ClientUnaryCall;
  getRun(
    request: GetRunRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Run) => void,
  ): ClientUnaryCall;
  getRun(
    request: GetRunRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Run) => void,
  ): ClientUnaryCall;
  /** WatchRun resumes events strictly after the supplied sequence. */
  watchRun(request: WatchRunRequest, options?: Partial<CallOptions>): ClientReadableStream<RunEvent>;
  watchRun(
    request: WatchRunRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<RunEvent>;
  /** CancelRun idempotently requests run cancellation. */
  cancelRun(request: CancelRunRequest, callback: (error: ServiceError | null, response: Run) => void): ClientUnaryCall;
  cancelRun(
    request: CancelRunRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Run) => void,
  ): ClientUnaryCall;
  cancelRun(
    request: CancelRunRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Run) => void,
  ): ClientUnaryCall;
}

export const RunServiceClient = makeGenericClientConstructor(
  RunServiceService,
  "agentro.execution.v1.RunService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): RunServiceClient;
  service: typeof RunServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
